# CONTRIBUTING — このリポジトリへの参加ルール

Cypher GitHub入門講座のリポジトリです。ここには「コミットメッセージの書き方」と「ブランチの名前の付け方」のルールが書いてあります。

> ⚠️ **初参加の方へ**：このページを全部覚える必要はありません。
> 当日は [docs/04_branch_pr.md](docs/04_branch_pr.md) の手順をなぞるだけで、自動的にこのルールに沿った形になります。
> ここは「なぜそのルールなのか」を後から知りたくなった時に読むページです。

---

## なぜルールがあるのか

Gitの履歴は**未来の自分と他人への手紙**です。

半年後のあなたが「この変更、なんでやったんだっけ？」と履歴を見た時に、
`update` `fix` `aaa` と並んでいたら何も分かりません。

実務のチームでは、この履歴を見て「どのバージョンまで戻すか」「どの変更が原因でバグが出たか」を判断します。
だから、**読める履歴を書く**ためのルールがあります。

---

## ブランチ命名規則

> **ブランチ**＝ 作業用の分岐した道。本線（`main`）を壊さずに作業するためのコピーです。

### 形式

```
<種類>/<GitHub ID>-<何をするか>
```

### 例

| ブランチ名 | 意味 |
|---|---|
| `feat/taro-yamada-profile` | taro-yamada が自己紹介ページを追加する |
| `docs/hanako-s-fix-typo` | hanako-s がドキュメントの誤字を直す |
| `fix/taro-yamada-broken-link` | taro-yamada がリンク切れを直す |

### 種類の一覧

| 種類 | 使う場面 |
|---|---|
| `feat` | 新しく何かを追加する（**今日のハンズオンはこれ**） |
| `fix` | 壊れているものを直す |
| `docs` | ドキュメントだけの変更 |
| `chore` | 設定ファイルなど、機能に関係ない雑務 |

### ルールの理由

- **GitHub IDを入れる**：30人が同時に作業するので、誰のブランチか一目で分かるようにするため
- **英数字とハイフンのみ**：日本語のブランチ名は、環境によって文字化けしたりコマンドが通らなくなります
- **スペースを使わない**：ターミナルではスペースが「区切り」の意味になるため、名前に入れると壊れます

### 今日のあなたのブランチ名

```
feat/<あなたのGitHub ID>-profile
```

例：GitHub IDが `taro-yamada` なら `feat/taro-yamada-profile`

---

## コミットメッセージ規則（Conventional Commits）

> **コミット**＝ 変更の記録を1つ確定させること。「セーブポイントを作る」イメージです。

### 形式

```
<種類>: <何をしたか（日本語でOK）>
```

種類の後ろは **半角コロン + 半角スペース** です。ここを間違えるとルール違反になります。

### 例

| ✅ 良い例 | ❌ 悪い例 | 悪い理由 |
|---|---|---|
| `feat: taro-yamada の自己紹介を追加` | `update` | 何を更新したか分からない |
| `docs: READMEのリンク切れを修正` | `修正しました` | 何を修正したか分からない |
| `fix: メンバー一覧の表示崩れを修正` | `aaa` | 何も伝わらない |
| `chore: .gitignore に .env を追加` | `feat:自己紹介追加` | コロンの後にスペースがない |

### 種類の一覧（ブランチと同じ）

| 種類 | 使う場面 |
|---|---|
| `feat` | 新しく何かを追加した |
| `fix` | 壊れていたものを直した |
| `docs` | ドキュメントだけ変えた |
| `chore` | 設定ファイルなどの雑務 |

### 書き方のコツ

**「このコミットを取り消したら何が失われるか」を書く。**

- ❌ `feat: ファイルを追加` → 何のファイル？
- ✅ `feat: taro-yamada の自己紹介ページを追加` → 取り消すと自己紹介が消える、と分かる

**1コミット＝1つの意味。**

「自己紹介を追加」と「READMEの誤字を修正」は別のコミットにします。
片方だけ取り消したくなった時に、まとめてあると取り消せません。

### 今日のあなたのコミットメッセージ

```
feat: <あなたのGitHub ID> の自己紹介を追加
```

---

## Pull Request のルール

> **Pull Request（PR）**＝「この変更を本線に取り込んでください」というお願いの投稿。

1. PRを作ると、テンプレート（穴埋め式の文章）が自動で入ります
2. **項目を消さずに、埋められるところだけ埋めてください**
3. 分からない項目は「分かりません」と書いてOKです。むしろそう書いてくれた方が助かります
4. タイトルはコミットメッセージと同じ形式：`feat: taro-yamada の自己紹介を追加`

### レビューについて

> **レビュー**＝ 他の人が変更内容を確認すること。

- 講師が確認して、問題なければ **Approve**（承認）します
- 直してほしい箇所があればコメントが付きます
- **コメントが付いた＝ダメだった、ではありません。** 「ここをこうするともっと良くなる」という提案です
- 実務では、コメントゼロでmergeされるPRの方が珍しいです

---

## やってはいけないこと

| やらないこと | 理由 |
|---|---|
| `main` ブランチに直接コミット・push | 全員が使う本線です。必ずブランチを切ってPRを出してください |
| `git push --force` | 他の人の作業を消してしまう可能性があります。詳細は [docs/05](docs/05_extra_conflict_revert.md) |
| 他の人の `members/*.md` を編集する | 自分のファイルだけを触ってください |
| APIキー・パスワード・`.env` をコミット | 一度上げると完全には消せません。詳細は [docs/06_safety.md](docs/06_safety.md) |

**やってしまっても怒られません。** 直し方も含めて講座の内容です。落ち着いて講師に伝えてください。

---

## 参考

- [Conventional Commits 公式](https://www.conventionalcommits.org/ja/v1.0.0/) — 今日使うルールの元になっている国際的な規約
- [docs/03_basic_commands.md](docs/03_basic_commands.md) — コマンドの逆引き辞典
